<?xml version="1.0" encoding="UTF-8"?>
<cursor xmlns="http://bio4j.ru/biodef-repo/store"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://bio4j.ru/biodef-repo/store http://bio4j.ru/biodef-repo/store/store-v10.xsd">
  <SQL action="select">
    <text>
      <![CDATA[
SELECT a.id_prop as id, a.prop || ' - ' || a.adesc as caption
  FROM givc_org.prop a
WHERE ((:query is null) or (upper(a.prop) like '%'||upper(:query)||'%')
            or (upper(a.adesc) like '%'||upper(:query)||'%'))
ORDER BY caption
      ]]>
    </text>
  </SQL>

  <fields>
    <field name="id" type="integer" pk="true"/>
    <field name="caption" type="string"/>
  </fields>
</cursor>